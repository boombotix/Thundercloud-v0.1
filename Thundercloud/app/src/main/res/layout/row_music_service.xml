<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:paddingBottom="@dimen/default_margin_half"
    android:paddingTop="@dimen/default_margin_half">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/music_service_logo_description"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/name"
        style="@style/TextAppearance.Thundercloud.Title"
        android:layout_marginLeft="@dimen/default_margin"
        android:layout_marginStart="@dimen/default_margin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/logo"
        android:layout_toRightOf="@+id/logo"
        android:layout_toLeftOf="@id/connect"
        android:layout_toStartOf="@id/connect"
        android:text="Spotify" />

    <boombotix.com.thundercloud.ui.view.ConnectButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/connected"
        android:id="@+id/connect"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        style="@style/Thundercloud.Button" />
</RelativeLayout>